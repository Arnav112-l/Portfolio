/** Site config — routes and display toggles */

export const routes = {
  about: true,
  work: true,
  contact: true,
};

export const display = {
  currentLocation: true,
  time: true,
  themeSwitcher: true,
};
